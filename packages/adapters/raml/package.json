{
  "name": "@sketch-test/adapter-raml",
  "version": "0.1.0",
  "private": true,
  "description": "RAML 1.0 adapter — produces CanonicalApiModel",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "@sketch-test/canonical-api-model": "workspace:*",
    "@sketch-test/contracts-common": "workspace:*",
    "yaml": "^2"
  },
  "devDependencies": {
    "@sketch-test/tsconfig": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
