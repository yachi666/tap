{
  "name": "@sketch-test/control-plane",
  "version": "0.0.0",
  "private": true,
  "description": "SketchTest Control Plane — Fastify API server (M0 skeleton)",
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "dev": "tsx --watch src/main.ts",
    "start": "tsx src/main.ts",
    "check": "tsc --noEmit",
    "lint": "biome check",
    "test": "vitest run",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "@sketch-test/contracts-common": "workspace:*",
    "@sketch-test/canonical-api-model": "workspace:*",
    "@sketch-test/runner-protocol": "workspace:*",
    "@sketch-test/test-dsl": "workspace:*",
    "@sketch-test/adapter-openapi": "workspace:*",
    "fastify": "^5.9.0",
    "pg": "^8.13.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
